return {
  "akinsho/bufferline.nvim",
  opts = {
    options = {
      always_show_bufferline = true, -- Keep bufferline visible even with single buffer
      show_buffer_close_icons = false, -- Clean look without close buttons
      show_close_icon = false, -- No close icon on the right
      separator_style = "thin", -- Clean separator style
    },
  },
}